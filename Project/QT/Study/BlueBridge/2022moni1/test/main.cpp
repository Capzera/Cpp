#include <bits/stdc++.h>
using namespace std;
int main(){
    cin.tie(nullptr)->sync_with_stdio(false);
    long long ans = 0;
    for (int i = 1; i < 100; i++) {
        ans += i * i;
    }
    if ("AAC" < "AC") cout << 1 << endl;
    return 0;
}
