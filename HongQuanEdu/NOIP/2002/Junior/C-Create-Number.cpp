/*
给出一个整数 n 和 k 个变换规则。
规则：
一位数可变换成另一个一位数。
规则的右部不能为零。
例如：n = 234, k = 2。有以下两个规则：
2⟶5。
3⟶6。
上面的整数 234 经过变换后可能产生出的整数为（包括原数）:
234。 534。 264。 564。
共 4 种不同的产生数。
现在给出一个整数 n 和 k 个规则。求出经过任意次的变换（0 次或多次），能产生出多少个不同整数。
仅要求输出个数。
*/
#include <bits/stdc++.h>
using namespace std;
int main() {
    string str;
    int k;
    cin >> str >> k;
    for (int i = 0; i < k; i++) {

    }
    system("pause");
    return 0;
}