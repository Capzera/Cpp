#include <bits/stdc++.h>
using namespace std;
int main() {
	vector<int> a = {1, 3, 2, 4, 6};
	sort(a.begin(), a.end(), greater<int>());
	for (auto& x : a) {
		cout << x << " ";
	}
	swap()
	return 0;
}
