#include <bits/stdc++.h>
using namespace std;
int main() {
    int n, x, sum = 0;
    cin >> n;
    vector<int> nums(n), cnt(n);
    long long ans = 0;
    for (auto& x : nums) cin >> x;
    for (int i = 0; i < n; i++) {
        if ()
    }
    cout << ans << endl;
    system("pause");
    return 0;
}