#include <bits/stdc++.h>
using namespace std;
int main() {
    int a, b, r, v, m;
    cin >> a >> b >> r >> v >> m;
    cout << (4 * a + b) * r << " " << (6 * a + b) * v << " " << (6 * a + b) * m << endl;
    system("pause");
    return 0;
}