#include <bits/stdc++.h>
using namespace std;

int main() {
	unsigned int s.size() = s.length();
	s.empty();
	sort(s.begin(), s.end());
	vector<string> a;
	sort(a.begin(), a.end());
	to_string() // 整数 -> str
	stoi() -> string to int
	stod() -> string to double
	stoll() -> string to long long
	isdigit() -> 判断是否为数字
	isalpha() -> 判断是否为字母
	isspace() -> 判断是否为空格
	isupper() -> 判断是否为大写
	islower() -> 判断是否为小写
	toupper() -> 全转大写
	tolower() -> 全转小写
	s.substr(x); -> 从x位置开始截取到末尾
	s.substr(x, y); -> 从x开始截取y个字符
	reverse(s.begin(), s.end()) 反转
	s.back() -> 获取最后一位字符
	sizeof() -> 获取字符串占用字节
	
	return 0;
}
/*
WWWWWWWWWWWWWWWWWWWWWWLW
*/
