#include <bits/stdc++.h>
using namespace std;
int main() {
    int num[5];
    memset(num, 127, sizeof(num));
    for (int i = 0; i < 5; i++) cout << num[i] << " ";
    cout << endl << 0x127 << endl;2139062143
    system("pause");
    return 0;
}